[package]
name = "wyrtloom-store-sqlite"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
wyrtloom-core = { path = "../wyrtloom/crates/core" }
# Portable alternative: wyrtloom-core = { git = "https://github.com/codepilots/wyrtloom.git", rev = "<sha>" }
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = "1"
thiserror = "1"
